---
description: Run any ESPHome command (e.g., compile, config, upload) esphome <COMMAND> <YAML_FILE>
---

1. Ensure you have an ESPHome configuration file (e.g., `ventosync.yaml`).
// turbo
2. Run the ESPHome command:
```powershell
esphome <COMMAND> <YAML_FILE>
```